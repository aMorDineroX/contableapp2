/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // Configuration HTTPS gérée via le script de développement
}

module.exports = nextConfig